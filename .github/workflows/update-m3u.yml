name: Update YouTube M3U Playlist

on:
  schedule:
    # S'exécute toutes les 3 heures
    - cron: '0 */3 * * *'
  # Permet aussi de déclencher le script manuellement depuis l'onglet Actions sur GitHub
  workflow_dispatch:

jobs:
  update-playlist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v4

      - name: Configuration de Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Installation des dépendances (yt-dlp)
        run: |
          python -m pip install --upgrade pip
          pip install yt-dlp

      - name: Exécution du script de mise à jour
        run: python update_playlist.py

      - name: Commit et Push des modifications
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add playlist.m3u
          # Ne commit que s'il y a des changements
          git diff --staged --quiet || (git commit -m "Auto-update playlist.m3u [skip ci]" && git push)
